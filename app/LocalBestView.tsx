import React from 'react'
import { StyleSheet, Text } from 'react-native'
import { SafeAreaView } from 'react-native-safe-area-context'

const LocalBestView = () => {
  return (
    <SafeAreaView>
      <Text>LocalBestView</Text>
    </SafeAreaView>
  )
}

export default LocalBestView

const styles = StyleSheet.create({})